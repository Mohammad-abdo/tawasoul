/doctor/conversations:
  get:
    tags: [Doctor Conversations]
    summary: Get all conversations (inbox)
    operationId: getDoctorConversations
    security:
      - BearerAuth: []
    parameters:
      - name: page
        in: query
        required: false
        schema:
          type: integer
          default: 1
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          default: 20
    responses:
      "200":
        description: List of conversations
        content:
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                data:
                  type: object
                  properties:
                    conversations:
                      type: array
                      items:
                        $ref: "#/components/schemas/DoctorConversation"
                    pagination:
                      $ref: "#/components/schemas/Pagination"

  post:
    tags: [Doctor Conversations]
    summary: Create a new conversation with a user
    operationId: createConversation
    security:
      - BearerAuth: []
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              - userId
            properties:
              userId:
                type: string
                format: uuid
                description: The user ID to start a conversation with
    responses:
      "201":
        description: Conversation created (or existing one returned)
        content:
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                data:
                  $ref: "#/components/schemas/DoctorConversation"
      "400":
        description: Bad request
      "404":
        description: User not found

/doctor/conversations/{userId}:
  get:
    tags: [Doctor Conversations]
    summary: Get messages in a specific conversation with a user
    operationId: getConversationMessages
    security:
      - BearerAuth: []
    parameters:
      - name: userId
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: The user ID
      - name: page
        in: query
        required: false
        schema:
          type: integer
          default: 1
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          default: 50
    responses:
      "200":
        description: Messages in conversation
        content:
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                data:
                  type: object
                  properties:
                    conversationId:
                      type: string
                      format: uuid
                    messages:
                      type: array
                      items:
                        $ref: "#/components/schemas/DoctorMessage"
                    pagination:
                      $ref: "#/components/schemas/Pagination"

/doctor/conversations/{conversationId}/details:
  get:
    tags: [Doctor Conversations]
    summary: Get a single conversation by ID with full details
    operationId: getConversationById
    security:
      - BearerAuth: []
    parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: The conversation ID
    responses:
      "200":
        description: Conversation details
        content:
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                data:
                  $ref: "#/components/schemas/DoctorConversation"
      "403":
        description: Forbidden
      "404":
        description: Not found

/doctor/conversations/{conversationId}:
  delete:
    tags: [Doctor Conversations]
    summary: Delete a conversation by ID
    operationId: deleteConversation
    security:
      - BearerAuth: []
    parameters:
      - name: conversationId
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: The conversation ID
    responses:
      "200":
        description: Conversation deleted
        content:
          application/json:
            schema:
              type: object
              properties:
                success:
                  type: boolean
                data:
                  type: object
                  properties:
                    message:
                      type: string
      "403":
        description: Forbidden
      "404":
        description: Not found